#!/bin/sh

set -e

cp makeR/res/wercker.yml .
Rscript -e 'devtools::use_build_ignore(c("wercker.yml", "_builds", "_projects", "_steps"))'
Rscript -e 'devtools:::add_git_ignore(c("_builds", "_projects", "_steps"))'
git add wercker.yml .Rbuildignore .gitignore
git commit -m "init wercker"
